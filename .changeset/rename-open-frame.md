---
"@open-frame/core": major
"@open-frame/cli": major
---

Rename packages from `@open-studio/*` to `@open-frame/*`. The CLI binary is now `open-frame`, the config file is `open-frame.config.ts`, and the exported config type is `OpenFrameConfig`.
